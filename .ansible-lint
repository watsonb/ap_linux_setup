---

exclude_paths:
  - roles
  - plays/roles
  - utils/roles
  - collections
  - molecule
  - .gitlab-ci.yml

parseable: true

quiet: true

# rulesdir:
#  - ./rule/directory/

skip_list:
  - skip_ansible_lint

# tags:
#  - run_this_tag

offline: true

extra_vars:
  variable_host: localhost

use_default_rules: true

verbosity: 0
